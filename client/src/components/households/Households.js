import React from "react";

const Households = () => {
  return (
    <div>
      One guiding principle is we let our admins create the postcode groups or
      areas So I need area admin first.
    </div>
  );
};

export default Households;
