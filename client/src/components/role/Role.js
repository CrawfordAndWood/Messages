import React from "react";
import PropTypes from "prop-types";

const Role = props => {
  return (
    <div>
      <h2>Role Management</h2>
      <p>
        Below is going to be a table where we can add, edit and delete roles. I
        can have one save changes button that changes colour if any changes have
        been made
      </p>
    </div>
  );
};

Role.propTypes = {};

export default Role;
